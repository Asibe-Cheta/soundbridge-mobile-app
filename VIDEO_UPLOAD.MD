Before writing any code, explore the full codebase to understand the existing file structure, component patterns, Supabase schema, existing upload flows for audio and images, existing profile page structure including the Portfolio tab on both mobile and web, ASK THE WEB TEAM TO BE 100% SURE ON EXISTING CLOUDFLARE INFRASTRUCTURE, existing push notification infrastructure and which services are used for it, existing modal and onboarding flow patterns, existing environment variable setup, and existing Cloudflare R2 integration. Match all new code to existing patterns and conventions. Do not duplicate anything that already exists. Do not assume any library or service is in use without first confirming it exists in the codebase.

You are implementing a Video Upload feature for SoundBridge. Videos live inside the existing Portfolio tab on the creator profile page. Do not create a new tab. Find the existing Portfolio tab component on both the React Native app and the Next.js web app and add video support inside it alongside whatever already exists there.

Here are the full requirements:

INFRASTRUCTURE: Use Cloudflare Stream for all video processing, transcoding and delivery. Do not use Cloudflare R2 for video files. All video uploads go through a server side API route that calls the Cloudflare Stream API using CLOUDFLARE_STREAM_API_TOKEN and CLOUDFLARE_ACCOUNT_ID environment variables. Never expose these tokens client side. Add CLOUDFLARE_STREAM_API_TOKEN, CLOUDFLARE_ACCOUNT_ID and CLOUDFLARE_STREAM_WEBHOOK_SECRET to the existing environment variable files and to Expo constants config. Check whether these variables already exist before adding them.

DATABASE: Create a videos table in Supabase with these fields: id uuid primary key default uuid_generate_v4(), user_id uuid references profiles(id) on delete cascade, cloudflare_stream_id text not null, playback_url text not null, thumbnail_url text, title text, duration integer stored in seconds, status text default processing with a check constraint allowing only the values processing, ready and failed, created_at timestamptz default now(). Apply Row Level Security. Creators can insert, update and delete only their own videos. Both authenticated and unauthenticated users can read videos where status is ready. No other access is permitted. Check the existing Supabase schema before creating this table to ensure there is no existing videos table or similar structure already in place.

UPLOAD RULES: Maximum 3 videos per creator profile. If the creator already has 3 videos, block the upload before anything is sent to the server and show a specific message telling them they have reached the maximum and must delete an existing video before uploading a new one. Maximum duration is 90 seconds. Validate duration on the client side before upload and reject with a specific message if exceeded. Maximum file size is 50MB. Accepted formats are MP4, MOV and AVI only. Validate both format and file size on the client side before upload and show specific messages for each violation.

UPLOAD FLOW: When a creator selects a video file, run all client side validations first. If any validation fails, show the specific error message and stop. If all validations pass, send the file to the server side API route which uploads it to Cloudflare Stream. While uploading and while Cloudflare Stream is processing, show a clear progress indicator to the creator with appropriate loading state messaging. Do not display the video on the profile or save a completed record to the database until Cloudflare Stream confirms the video is fully ready. Use Cloudflare Stream webhooks to listen for the video.ready event. When received, update the video status to ready in the Supabase videos table and populate playback_url and thumbnail_url from the webhook payload. If Cloudflare Stream reports a processing error via webhook, update the status to failed in the database and show the creator a user friendly error message telling them the video could not be processed and they should try uploading again.

PORTFOLIO TAB INTEGRATION: Inside the existing Portfolio tab on the creator profile, add a Videos section. Display uploaded videos in a grid layout showing the thumbnail, a duration badge overlaid on the thumbnail, and the title below if one exists. Tapping or clicking a video opens it in a full screen player. Use the Cloudflare Stream JavaScript player on the Next.js web app. On React Native use whichever video player library is already installed in the project, check package.json before adding any new dependency. The player uses HLS adaptive bitrate streaming delivered automatically by Cloudflare Stream. Videos must not appear anywhere else in the app including the main feed, For You tab, Following tab, Events tab or any other screen.

DELETION: Creators can delete their own videos from the Portfolio tab. On deletion, first call the Cloudflare Stream API to delete the video from Cloudflare entirely. Then delete the record from the Supabase videos table. This is a hard delete, not a soft delete. Always show a confirmation prompt before proceeding with deletion.

FEATURE ANNOUNCEMENT MODAL: When an existing user opens the app for the first time after this feature is deployed, show a one time modal announcing that video uploads are now live on SoundBridge. The modal should explain that creators can now upload up to 3 videos of up to 90 seconds each to their Portfolio to showcase their best work to fans, collaborators and industry professionals. It should encourage them to choose high quality videos that truly represent what they are capable of. Include a call to action button that takes them directly to the video upload section of their Portfolio. Store a flag in AsyncStorage or whichever local storage pattern already exists in the codebase to ensure this modal only shows once per user and never again after they have dismissed it.

UPLOAD SUGGESTION FLOW: After a creator completes a music upload or any other portfolio action, check whether they have uploaded any videos yet. If they have zero videos, show a suggestion prompt encouraging them to add a video to their portfolio to make their profile more complete and professional. This suggestion should follow the same pattern as the existing distribution suggestion shown after music uploads. Find that existing suggestion component and follow the same design and behaviour pattern exactly. The suggestion should not be intrusive and should be easily dismissable.

PUSH NOTIFICATIONS: When a creator successfully uploads a video and it becomes ready, send them a push notification confirming their video is live on their profile. Before implementing this, check the existing codebase to identify which push notification service and infrastructure is already in use. Use exactly that existing infrastructure and pattern. Do not introduce any new notification service or library.

ERROR HANDLING: Every error at every stage must show a specific human readable message. Never show raw JSON, raw error objects, stack traces, HTTP status codes or any technical output to the user under any circumstances. For client side validation errors show exactly which rule was violated. For upload failures show a friendly message telling the user the upload failed and to try again. For processing failures show a friendly message telling the user their video could not be processed and to try again. For deletion failures show a friendly message telling the user the deletion failed and to try again. For any backend error caused by infrastructure issues such as Cloudflare API failures, billing issues, service outages or any unexpected server error, show this exact message to the user: We are currently running some improvements to our video feature. Please bear with us and try again shortly. This message must appear for any 5xx error, any Cloudflare API error response, any network timeout from the server side API route, and any unhandled exception in the video upload or processing pipeline. Log the actual technical error to the server console for debugging but never expose it to the user.

MOBILE AND WEB PARITY: Every part of this feature must work identically on the React Native Expo mobile app and the Next.js web app. Upload, validation, Portfolio display, video playback, deletion, the announcement modal, the suggestion flow and push notifications must all function correctly on both platforms. Check both platforms before finalising any component or API route.
