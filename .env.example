# ==============================================================================
# SOUNDBRIDGE MOBILE APP - ENVIRONMENT VARIABLES
# ==============================================================================
#
# This is an example environment file. Copy this to .env.local and fill in
# your actual values. NEVER commit .env.local to Git (it's in .gitignore).
#
# ==============================================================================

# ------------------------------------------------------------------------------
# WISE API CONFIGURATION (Production/Live)
# ------------------------------------------------------------------------------
# Wise (formerly TransferWise) is used for global creator payouts to 160+ countries
# Documentation: https://docs.wise.com/api-docs/api-reference/transfer
#
# IMPORTANT: These are PRODUCTION/LIVE credentials - handle with care!
# DO NOT use sandbox values in production environment.
# ------------------------------------------------------------------------------

# Wise API Token (REQUIRED)
# Get this from: https://wise.com/settings/api-tokens
# Format: 9e0fac28-xxxx-xxxx-xxxx-xxxxxxxxxxxx (UUID format)
# Scopes needed: transfers.read, transfers.write, balances.read
WISE_API_TOKEN=your_live_wise_api_token_here

# Wise Environment (REQUIRED)
# Values: "live" (production) or "sandbox" (testing)
# PRODUCTION: Use "live" for real transfers
WISE_ENVIRONMENT=live

# Wise API Base URL (REQUIRED)
# Production: https://api.wise.com
# Sandbox: https://api.sandbox.transferwise.tech
# This should match WISE_ENVIRONMENT setting
WISE_API_URL=https://api.wise.com

# Wise Webhook Secret (REQUIRED for webhook verification)
# Generate a random 32-character string for security
# Used to verify webhook requests are actually from Wise
# Generate with: openssl rand -hex 32
# Or use: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
WISE_WEBHOOK_SECRET=your_32_character_webhook_secret_here

# Wise Profile ID (REQUIRED)
# Your Wise business profile ID
# Get this from: https://wise.com/settings/profiles
# Or get via API: curl https://api.wise.com/v1/profiles -H "Authorization: Bearer YOUR_TOKEN"
# Format: Numeric ID (e.g., 12345678)
WISE_PROFILE_ID=your_wise_profile_id_here

# Wise Webhook Subscription ID (OPTIONAL - auto-generated)
# Created when you run: node scripts/create-wise-webhook.js
# This is the webhook subscription ID from Wise API
# Used for testing and managing the webhook
# Format: UUID (e.g., 12345678-1234-1234-1234-123456789012)
WISE_WEBHOOK_SUBSCRIPTION_ID=your_subscription_id_here

# ------------------------------------------------------------------------------
# SUPABASE CONFIGURATION
# ------------------------------------------------------------------------------
# Add your existing Supabase credentials here if not already configured
# SUPABASE_URL=your_supabase_url_here
# SUPABASE_ANON_KEY=your_supabase_anon_key_here

# ------------------------------------------------------------------------------
# STRIPE CONFIGURATION (if applicable)
# ------------------------------------------------------------------------------
# Add your existing Stripe credentials here if not already configured
# STRIPE_PUBLISHABLE_KEY=your_stripe_publishable_key_here
# STRIPE_SECRET_KEY=your_stripe_secret_key_here

# ------------------------------------------------------------------------------
# OTHER API KEYS
# ------------------------------------------------------------------------------
# Add any other API keys or environment-specific configuration here
